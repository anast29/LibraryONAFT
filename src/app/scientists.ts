export interface Scientists {
    image: Array<string>;
    name: Array<string>;
    birthday: string;
    deathday: string;
    short_description: Array<string>;
    bio: Array<string>;
    works: Array<string>;
}
